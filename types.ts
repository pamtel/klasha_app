export interface ContactTypes {
  name: string;
  team: string;
  img: string;
}

export interface PageType { 
  path: string; 
  icon: string;
}

export interface ScheduleType {
  id: number;
  meeting: string;
  description: string;
  time: string;
  color: string;
}